리스트, 튜플, 딕셔너리 , 비트개념
if, for, while


c언어에서는 같은 자료형들을 묶어놓은 것들 => 배열

파이썬의 배열은 리스트가 대표적이다.

[] 변경 가능한 리스트 => 슬라이싱, 연산자 사용 가능,
※리스트의 항목: 정수, 실수, 문자열, 문자열과 숫자 혼용
len, insert(인덱스,값), extend(리스트), append(), sort(), del , remove() 앞에서 부터 하나 없애줌
pop() < 두가지 용도, clear() < 전부 비우기
() 변경 불가능한 
예) 튜플 괄호를 안 써도 됨
a = (1,2)
a = 1,2
{} 키와 값이 한 쌍을 이루고 있는 딕셔너리 get(), keys(), items(), values()


비트 이진수를 칭하는 말이다.

쉽게 말해서 평소에 우리가 10진수를 쓰는데 컴퓨터는 2진수를 쓰고 2진수로 나타내는 단위를
비트라고 한다.

8비트 => 1byte인데 이걸 용량을 다 설명하면 끝이 없기에 이정도만 하고 
그래서 비트란 배열을 비유해서 설명을 한다.


알고리즘의 3가지 구조
1. 순차 구조  => 다른 것 없이 위에서 부터 순차적으로 실행
2. 선택 구조  => if 조건에 의해 케이스가 나뉘는 것
3. 반복 구조  => for , while 을 통해 구조가 동일하게 반복 되는 것

선택 구조에는 

if , if else , if elif else 크게 3가지


반복 구조
반복문이 필요한 이유 => 간단하고 반복되는 문장을 일일이 코딩하여 시키면 코드가 난잡해지며
오류를 잡기 힘들기 때문이다.

크게 반복에는 두 가지를 이용한다.
for, while이 있다.

for 변수 in 시퀀스:

c언어 와는 다르게 변수 선언을 미리 안 해도 된다.
시퀀스 : range , 리스트, 튜플 , 문자열 다 가능
시퀀스 뜻으로는 연속적인 자료들이라고 할 수 있다.
range
3가지
range(초기, 종료, 증가)
range(초기, 종료)
range(종료)

while 조건:

break, continue 문